services:
  treedx-node-a:
    environment:
      TREEDX_RUNTIME_CPU_BUDGET: ${TREEDX_RUNTIME_CPU_BUDGET:-4}
      TREEDX_RUNTIME_MEMORY_BUDGET_MB: ${TREEDX_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDX_CACHE_MEMORY_FRACTION: ${TREEDX_CACHE_MEMORY_FRACTION:-0.35}
      TREEDX_REPOSITORY_QUERY_POOL_SIZE: ${TREEDX_REPOSITORY_QUERY_POOL_SIZE:-16}
      TREEDX_WORKSPACE_WORKER_POOL_SIZE: ${TREEDX_WORKSPACE_WORKER_POOL_SIZE:-8}
      TREEDX_GRAPH_WORKER_POOL_SIZE: ${TREEDX_GRAPH_WORKER_POOL_SIZE:-8}
      TREEDX_SNAPSHOT_WORKER_POOL_SIZE: ${TREEDX_SNAPSHOT_WORKER_POOL_SIZE:-4}
      TREEDX_REPOSITORY_QUERY_MAX_QUEUE: ${TREEDX_REPOSITORY_QUERY_MAX_QUEUE:-2000}
      TREEDX_WORKSPACE_MUTATION_MAX_QUEUE: ${TREEDX_WORKSPACE_MUTATION_MAX_QUEUE:-1000}
      TREEDX_GRAPH_MAX_QUEUE: ${TREEDX_GRAPH_MAX_QUEUE:-500}
      TREEDX_SNAPSHOT_MAX_QUEUE: ${TREEDX_SNAPSHOT_MAX_QUEUE:-200}
      TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS: ${TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:-30000}
      TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS: ${TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:-30000}
      TREEDX_GRAPH_QUEUE_TIMEOUT_MS: ${TREEDX_GRAPH_QUEUE_TIMEOUT_MS:-45000}
      TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS: ${TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS:-60000}
      TREEDX_FEDERATION_LOAD_AWARE_READS: ${TREEDX_FEDERATION_LOAD_AWARE_READS:-true}
      TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS: ${TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS:-2000}

  treedx-node-b:
    environment:
      TREEDX_RUNTIME_CPU_BUDGET: ${TREEDX_RUNTIME_CPU_BUDGET:-4}
      TREEDX_RUNTIME_MEMORY_BUDGET_MB: ${TREEDX_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDX_CACHE_MEMORY_FRACTION: ${TREEDX_CACHE_MEMORY_FRACTION:-0.35}
      TREEDX_REPOSITORY_QUERY_POOL_SIZE: ${TREEDX_REPOSITORY_QUERY_POOL_SIZE:-16}
      TREEDX_WORKSPACE_WORKER_POOL_SIZE: ${TREEDX_WORKSPACE_WORKER_POOL_SIZE:-8}
      TREEDX_GRAPH_WORKER_POOL_SIZE: ${TREEDX_GRAPH_WORKER_POOL_SIZE:-8}
      TREEDX_SNAPSHOT_WORKER_POOL_SIZE: ${TREEDX_SNAPSHOT_WORKER_POOL_SIZE:-4}
      TREEDX_REPOSITORY_QUERY_MAX_QUEUE: ${TREEDX_REPOSITORY_QUERY_MAX_QUEUE:-2000}
      TREEDX_WORKSPACE_MUTATION_MAX_QUEUE: ${TREEDX_WORKSPACE_MUTATION_MAX_QUEUE:-1000}
      TREEDX_GRAPH_MAX_QUEUE: ${TREEDX_GRAPH_MAX_QUEUE:-500}
      TREEDX_SNAPSHOT_MAX_QUEUE: ${TREEDX_SNAPSHOT_MAX_QUEUE:-200}
      TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS: ${TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:-30000}
      TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS: ${TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:-30000}
      TREEDX_GRAPH_QUEUE_TIMEOUT_MS: ${TREEDX_GRAPH_QUEUE_TIMEOUT_MS:-45000}
      TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS: ${TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS:-60000}
      TREEDX_FEDERATION_LOAD_AWARE_READS: ${TREEDX_FEDERATION_LOAD_AWARE_READS:-true}
      TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS: ${TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS:-2000}

  treedx-node-c:
    environment:
      TREEDX_RUNTIME_CPU_BUDGET: ${TREEDX_RUNTIME_CPU_BUDGET:-4}
      TREEDX_RUNTIME_MEMORY_BUDGET_MB: ${TREEDX_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDX_CACHE_MEMORY_FRACTION: ${TREEDX_CACHE_MEMORY_FRACTION:-0.35}
      TREEDX_REPOSITORY_QUERY_POOL_SIZE: ${TREEDX_REPOSITORY_QUERY_POOL_SIZE:-16}
      TREEDX_WORKSPACE_WORKER_POOL_SIZE: ${TREEDX_WORKSPACE_WORKER_POOL_SIZE:-8}
      TREEDX_GRAPH_WORKER_POOL_SIZE: ${TREEDX_GRAPH_WORKER_POOL_SIZE:-8}
      TREEDX_SNAPSHOT_WORKER_POOL_SIZE: ${TREEDX_SNAPSHOT_WORKER_POOL_SIZE:-4}
      TREEDX_REPOSITORY_QUERY_MAX_QUEUE: ${TREEDX_REPOSITORY_QUERY_MAX_QUEUE:-2000}
      TREEDX_WORKSPACE_MUTATION_MAX_QUEUE: ${TREEDX_WORKSPACE_MUTATION_MAX_QUEUE:-1000}
      TREEDX_GRAPH_MAX_QUEUE: ${TREEDX_GRAPH_MAX_QUEUE:-500}
      TREEDX_SNAPSHOT_MAX_QUEUE: ${TREEDX_SNAPSHOT_MAX_QUEUE:-200}
      TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS: ${TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:-30000}
      TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS: ${TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:-30000}
      TREEDX_GRAPH_QUEUE_TIMEOUT_MS: ${TREEDX_GRAPH_QUEUE_TIMEOUT_MS:-45000}
      TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS: ${TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS:-60000}
      TREEDX_FEDERATION_LOAD_AWARE_READS: ${TREEDX_FEDERATION_LOAD_AWARE_READS:-true}
      TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS: ${TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS:-2000}

  treedx-profiler:
    environment:
      TREEDX_RUNTIME_CPU_BUDGET: ${TREEDX_RUNTIME_CPU_BUDGET:-4}
      TREEDX_RUNTIME_MEMORY_BUDGET_MB: ${TREEDX_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDX_CACHE_MEMORY_FRACTION: ${TREEDX_CACHE_MEMORY_FRACTION:-0.35}
      TREEDX_PROFILE_PROFILE_PURPOSE: ${TREEDX_PROFILE_PROFILE_PURPOSE:-performance}
      TREEDX_PROFILE_TARGET_PRIMARY_RPS: ${TREEDX_PROFILE_TARGET_PRIMARY_RPS:-100}
      TREEDX_PROFILE_FEDERATION_MODE: ${TREEDX_PROFILE_FEDERATION_MODE:-mirror_cluster}
      TREEDX_PROFILE_INCLUDE_FEDERATION: ${TREEDX_PROFILE_INCLUDE_FEDERATION:-true}
      TREEDX_PROFILE_CONCURRENCY: ${TREEDX_PROFILE_CONCURRENCY:-150}
      TREEDX_PROFILE_DURATION: ${TREEDX_PROFILE_DURATION:-10m}
      TREEDX_PROFILE_MINIMUM_MEASURED_DURATION: ${TREEDX_PROFILE_MINIMUM_MEASURED_DURATION:-10m}
      TREEDX_PROFILE_VALIDATION_PROBE_MODE: ${TREEDX_PROFILE_VALIDATION_PROBE_MODE:-sampled}
      TREEDX_PROFILE_PROBE_SAMPLING_RATE: ${TREEDX_PROFILE_PROBE_SAMPLING_RATE:-0.10}
      TREEDX_PROFILE_PERFORMANCE_WORKLOAD: ${TREEDX_PROFILE_PERFORMANCE_WORKLOAD:-read_mostly}
      TREEDX_PROFILE_FEDERATION_SPILLOVER_PROBE: ${TREEDX_PROFILE_FEDERATION_SPILLOVER_PROBE:-true}
      TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDX_PROFILE_MODEL_RECONCILIATION: ${TREEDX_PROFILE_MODEL_RECONCILIATION:-false}
      TREEDX_PROFILE_NEGATIVE_TESTS: ${TREEDX_PROFILE_NEGATIVE_TESTS:-false}
      TREEDX_PROFILE_METAMORPHIC_CHECKS: ${TREEDX_PROFILE_METAMORPHIC_CHECKS:-false}
      TREEDX_PROFILE_DELAYED_CONSISTENCY_CHECKS: ${TREEDX_PROFILE_DELAYED_CONSISTENCY_CHECKS:-false}
      TREEDX_PROFILE_PERMISSION_MATRIX: ${TREEDX_PROFILE_PERMISSION_MATRIX:-false}
