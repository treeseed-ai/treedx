services:
  treedb-node-a:
    environment:
      TREEDB_RUNTIME_CPU_BUDGET: ${TREEDB_RUNTIME_CPU_BUDGET:-4}
      TREEDB_RUNTIME_MEMORY_BUDGET_MB: ${TREEDB_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDB_CACHE_MEMORY_FRACTION: ${TREEDB_CACHE_MEMORY_FRACTION:-0.35}
      TREEDB_REPOSITORY_QUERY_POOL_SIZE: ${TREEDB_REPOSITORY_QUERY_POOL_SIZE:-16}
      TREEDB_WORKSPACE_WORKER_POOL_SIZE: ${TREEDB_WORKSPACE_WORKER_POOL_SIZE:-8}
      TREEDB_GRAPH_WORKER_POOL_SIZE: ${TREEDB_GRAPH_WORKER_POOL_SIZE:-8}
      TREEDB_SNAPSHOT_WORKER_POOL_SIZE: ${TREEDB_SNAPSHOT_WORKER_POOL_SIZE:-4}
      TREEDB_REPOSITORY_QUERY_MAX_QUEUE: ${TREEDB_REPOSITORY_QUERY_MAX_QUEUE:-2000}
      TREEDB_WORKSPACE_MUTATION_MAX_QUEUE: ${TREEDB_WORKSPACE_MUTATION_MAX_QUEUE:-1000}
      TREEDB_GRAPH_MAX_QUEUE: ${TREEDB_GRAPH_MAX_QUEUE:-500}
      TREEDB_SNAPSHOT_MAX_QUEUE: ${TREEDB_SNAPSHOT_MAX_QUEUE:-200}
      TREEDB_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS: ${TREEDB_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:-30000}
      TREEDB_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS: ${TREEDB_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:-30000}
      TREEDB_GRAPH_QUEUE_TIMEOUT_MS: ${TREEDB_GRAPH_QUEUE_TIMEOUT_MS:-45000}
      TREEDB_SNAPSHOT_QUEUE_TIMEOUT_MS: ${TREEDB_SNAPSHOT_QUEUE_TIMEOUT_MS:-60000}
      TREEDB_FEDERATION_LOAD_AWARE_READS: ${TREEDB_FEDERATION_LOAD_AWARE_READS:-true}
      TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDB_FEDERATION_REMOTE_LOAD_TTL_MS: ${TREEDB_FEDERATION_REMOTE_LOAD_TTL_MS:-2000}

  treedb-node-b:
    environment:
      TREEDB_RUNTIME_CPU_BUDGET: ${TREEDB_RUNTIME_CPU_BUDGET:-4}
      TREEDB_RUNTIME_MEMORY_BUDGET_MB: ${TREEDB_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDB_CACHE_MEMORY_FRACTION: ${TREEDB_CACHE_MEMORY_FRACTION:-0.35}
      TREEDB_REPOSITORY_QUERY_POOL_SIZE: ${TREEDB_REPOSITORY_QUERY_POOL_SIZE:-16}
      TREEDB_WORKSPACE_WORKER_POOL_SIZE: ${TREEDB_WORKSPACE_WORKER_POOL_SIZE:-8}
      TREEDB_GRAPH_WORKER_POOL_SIZE: ${TREEDB_GRAPH_WORKER_POOL_SIZE:-8}
      TREEDB_SNAPSHOT_WORKER_POOL_SIZE: ${TREEDB_SNAPSHOT_WORKER_POOL_SIZE:-4}
      TREEDB_REPOSITORY_QUERY_MAX_QUEUE: ${TREEDB_REPOSITORY_QUERY_MAX_QUEUE:-2000}
      TREEDB_WORKSPACE_MUTATION_MAX_QUEUE: ${TREEDB_WORKSPACE_MUTATION_MAX_QUEUE:-1000}
      TREEDB_GRAPH_MAX_QUEUE: ${TREEDB_GRAPH_MAX_QUEUE:-500}
      TREEDB_SNAPSHOT_MAX_QUEUE: ${TREEDB_SNAPSHOT_MAX_QUEUE:-200}
      TREEDB_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS: ${TREEDB_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:-30000}
      TREEDB_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS: ${TREEDB_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:-30000}
      TREEDB_GRAPH_QUEUE_TIMEOUT_MS: ${TREEDB_GRAPH_QUEUE_TIMEOUT_MS:-45000}
      TREEDB_SNAPSHOT_QUEUE_TIMEOUT_MS: ${TREEDB_SNAPSHOT_QUEUE_TIMEOUT_MS:-60000}
      TREEDB_FEDERATION_LOAD_AWARE_READS: ${TREEDB_FEDERATION_LOAD_AWARE_READS:-true}
      TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDB_FEDERATION_REMOTE_LOAD_TTL_MS: ${TREEDB_FEDERATION_REMOTE_LOAD_TTL_MS:-2000}

  treedb-node-c:
    environment:
      TREEDB_RUNTIME_CPU_BUDGET: ${TREEDB_RUNTIME_CPU_BUDGET:-4}
      TREEDB_RUNTIME_MEMORY_BUDGET_MB: ${TREEDB_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDB_CACHE_MEMORY_FRACTION: ${TREEDB_CACHE_MEMORY_FRACTION:-0.35}
      TREEDB_REPOSITORY_QUERY_POOL_SIZE: ${TREEDB_REPOSITORY_QUERY_POOL_SIZE:-16}
      TREEDB_WORKSPACE_WORKER_POOL_SIZE: ${TREEDB_WORKSPACE_WORKER_POOL_SIZE:-8}
      TREEDB_GRAPH_WORKER_POOL_SIZE: ${TREEDB_GRAPH_WORKER_POOL_SIZE:-8}
      TREEDB_SNAPSHOT_WORKER_POOL_SIZE: ${TREEDB_SNAPSHOT_WORKER_POOL_SIZE:-4}
      TREEDB_REPOSITORY_QUERY_MAX_QUEUE: ${TREEDB_REPOSITORY_QUERY_MAX_QUEUE:-2000}
      TREEDB_WORKSPACE_MUTATION_MAX_QUEUE: ${TREEDB_WORKSPACE_MUTATION_MAX_QUEUE:-1000}
      TREEDB_GRAPH_MAX_QUEUE: ${TREEDB_GRAPH_MAX_QUEUE:-500}
      TREEDB_SNAPSHOT_MAX_QUEUE: ${TREEDB_SNAPSHOT_MAX_QUEUE:-200}
      TREEDB_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS: ${TREEDB_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:-30000}
      TREEDB_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS: ${TREEDB_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:-30000}
      TREEDB_GRAPH_QUEUE_TIMEOUT_MS: ${TREEDB_GRAPH_QUEUE_TIMEOUT_MS:-45000}
      TREEDB_SNAPSHOT_QUEUE_TIMEOUT_MS: ${TREEDB_SNAPSHOT_QUEUE_TIMEOUT_MS:-60000}
      TREEDB_FEDERATION_LOAD_AWARE_READS: ${TREEDB_FEDERATION_LOAD_AWARE_READS:-true}
      TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDB_FEDERATION_REMOTE_LOAD_TTL_MS: ${TREEDB_FEDERATION_REMOTE_LOAD_TTL_MS:-2000}

  treedb-profiler:
    environment:
      TREEDB_RUNTIME_CPU_BUDGET: ${TREEDB_RUNTIME_CPU_BUDGET:-4}
      TREEDB_RUNTIME_MEMORY_BUDGET_MB: ${TREEDB_RUNTIME_MEMORY_BUDGET_MB:-4096}
      TREEDB_CACHE_MEMORY_FRACTION: ${TREEDB_CACHE_MEMORY_FRACTION:-0.35}
      TREEDB_PROFILE_PROFILE_PURPOSE: ${TREEDB_PROFILE_PROFILE_PURPOSE:-performance}
      TREEDB_PROFILE_TARGET_PRIMARY_RPS: ${TREEDB_PROFILE_TARGET_PRIMARY_RPS:-100}
      TREEDB_PROFILE_FEDERATION_MODE: ${TREEDB_PROFILE_FEDERATION_MODE:-mirror_cluster}
      TREEDB_PROFILE_INCLUDE_FEDERATION: ${TREEDB_PROFILE_INCLUDE_FEDERATION:-true}
      TREEDB_PROFILE_CONCURRENCY: ${TREEDB_PROFILE_CONCURRENCY:-150}
      TREEDB_PROFILE_DURATION: ${TREEDB_PROFILE_DURATION:-10m}
      TREEDB_PROFILE_MINIMUM_MEASURED_DURATION: ${TREEDB_PROFILE_MINIMUM_MEASURED_DURATION:-10m}
      TREEDB_PROFILE_VALIDATION_PROBE_MODE: ${TREEDB_PROFILE_VALIDATION_PROBE_MODE:-sampled}
      TREEDB_PROFILE_PROBE_SAMPLING_RATE: ${TREEDB_PROFILE_PROBE_SAMPLING_RATE:-0.10}
      TREEDB_PROFILE_PERFORMANCE_WORKLOAD: ${TREEDB_PROFILE_PERFORMANCE_WORKLOAD:-read_mostly}
      TREEDB_PROFILE_FEDERATION_SPILLOVER_PROBE: ${TREEDB_PROFILE_FEDERATION_SPILLOVER_PROBE:-true}
      TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE: ${TREEDB_FEDERATION_LOAD_AWARE_READ_PRESSURE:-low}
      TREEDB_PROFILE_MODEL_RECONCILIATION: ${TREEDB_PROFILE_MODEL_RECONCILIATION:-false}
      TREEDB_PROFILE_NEGATIVE_TESTS: ${TREEDB_PROFILE_NEGATIVE_TESTS:-false}
      TREEDB_PROFILE_METAMORPHIC_CHECKS: ${TREEDB_PROFILE_METAMORPHIC_CHECKS:-false}
      TREEDB_PROFILE_DELAYED_CONSISTENCY_CHECKS: ${TREEDB_PROFILE_DELAYED_CONSISTENCY_CHECKS:-false}
      TREEDB_PROFILE_PERMISSION_MATRIX: ${TREEDB_PROFILE_PERMISSION_MATRIX:-false}
