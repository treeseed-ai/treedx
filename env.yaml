entries:
  TREEDX_ALLOW_DEV_VERIFIER_IN_PROD:
    label: allow dev verifier in prod
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_ALLOW_DIRECT_EXEC_IN_PROD:
    label: allow direct exec in prod
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_API_HOST_PORT:
    label: api host port
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_API_MEMORY_LIMIT:
    label: api memory limit
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_ARTIFACT_INDEX_CACHE_MEMORY_WEIGHT:
    label: artifact index cache memory weight
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_ARTIFACT_INDEX_ENABLED:
    label: artifact index enabled
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_ARTIFACT_RETENTION_DAYS:
    label: artifact retention days
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_AUDIT_ASYNC:
    label: audit async
    group: audit
    description: TreeDX-owned audit environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_AUDIT_BATCH_SIZE:
    label: audit batch size
    group: audit
    description: TreeDX-owned audit environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_AUDIT_FLUSH_INTERVAL_MS:
    label: audit flush interval ms
    group: audit
    description: TreeDX-owned audit environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_AUDIT_QUEUE_MAX:
    label: audit queue max
    group: audit
    description: TreeDX-owned audit environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_AUTH_MODE:
    label: auth mode
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_AUTH_VERIFIER:
    label: auth verifier
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BACKUP_REMOTE_DIR:
    label: backup remote dir
    group: backup
    description: TreeDX-owned backup environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BACKUP_RETENTION_COUNT:
    label: backup retention count
    group: backup
    description: TreeDX-owned backup environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BACKUP_RETENTION_DAYS:
    label: backup retention days
    group: backup
    description: TreeDX-owned backup environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BASE_URL:
    label: base url
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BOOTSTRAP_TRUST_ACTOR_ID:
    label: bootstrap trust actor id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BOOTSTRAP_TRUST_CAPABILITIES:
    label: bootstrap trust capabilities
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BOOTSTRAP_TRUST_PATHS:
    label: bootstrap trust paths
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BOOTSTRAP_TRUST_REFS:
    label: bootstrap trust refs
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BOOTSTRAP_TRUST_REPO_IDS:
    label: bootstrap trust repo ids
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BOOTSTRAP_TRUST_TENANT_ID:
    label: bootstrap trust tenant id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_BUILD_TMP_DIR:
    label: build tmp dir
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CACHE_MEMORY_FRACTION:
    label: cache memory fraction
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CACHE_MIN_FREE_MEMORY_MB:
    label: cache min free memory mb
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CACHE_SAMPLING_INTERVAL_MS:
    label: cache sampling interval ms
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_FEDERATION_PROFILE_CONCURRENCY:
    label: ci federation profile concurrency
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_FEDERATION_PROFILE_DURATION:
    label: ci federation profile duration
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_FEDERATION_PROFILE_ENABLED:
    label: ci federation profile enabled
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_FEDERATION_PROFILE_MODES:
    label: ci federation profile modes
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_FEDERATION_PROFILE_SIZE:
    label: ci federation profile size
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_CONCURRENCY:
    label: ci profile concurrency
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_DURATION:
    label: ci profile duration
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_FIXTURE:
    label: ci profile fixture
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_ITERATIONS:
    label: ci profile iterations
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_LOAD_MODE:
    label: ci profile load mode
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_MODE:
    label: ci profile mode
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_SCENARIO:
    label: ci profile scenario
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CI_PROFILE_SIZE:
    label: ci profile size
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_ALLOW_ADMIN:
    label: conformance allow admin
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_ALLOW_DESTRUCTIVE:
    label: conformance allow destructive
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_ALLOW_INTERNAL:
    label: conformance allow internal
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_ARTIFACT_ID:
    label: conformance artifact id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_JOB_ID:
    label: conformance job id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_MIGRATION_ID:
    label: conformance migration id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_MIRROR_ID:
    label: conformance mirror id
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_NODE_ID:
    label: conformance node id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_PART_NUMBER:
    label: conformance part number
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_REF:
    label: conformance ref
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_REPO_ID:
    label: conformance repo id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_SNAPSHOT_ID:
    label: conformance snapshot id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_TMP:
    label: conformance tmp
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_UPLOAD_ID:
    label: conformance upload id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONFORMANCE_WORKSPACE_ID:
    label: conformance workspace id
    group: conformance
    description: TreeDX-owned conformance environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONTEXT_DEFAULT_MODE:
    label: context default mode
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_CONTEXT_MAX_TOKENS_DEFAULT:
    label: context max tokens default
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_DATA_DIR:
    label: data dir
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_DEV_ACTOR_ID:
    label: dev actor id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_DEV_HOT_RELOAD:
    label: dev hot reload
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_DEV_TENANT_ID:
    label: dev tenant id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_ENV:
    label: env
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_BACKEND:
    label: exec backend
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_CONTAINER_IMAGE:
    label: exec container image
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_MAX_CPU:
    label: exec max cpu
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_MAX_MEMORY_MB:
    label: exec max memory mb
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_MAX_PIDS:
    label: exec max pids
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_MICROVM_PROFILE:
    label: exec microvm profile
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_NETWORK_DEFAULT:
    label: exec network default
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_WORKER_HMAC_SECRET:
    label: exec worker hmac secret
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_WORKER_TIMEOUT_MS:
    label: exec worker timeout ms
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_WORKER_TOKEN:
    label: exec worker token
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_EXEC_WORKER_URL:
    label: exec worker url
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_ACCEPT_CHILD_REGISTRATION:
    label: federation accept child registration
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_AUTO_TRUST_PARENTS:
    label: federation auto trust parents
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_CATALOG_PUSH_ENABLED:
    label: federation catalog push enabled
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_CATALOG_SYNC_INTERVAL_MS:
    label: federation catalog sync interval ms
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_ENABLED:
    label: federation enabled
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_ENABLE_REMOTE_HTTP:
    label: federation enable remote http
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_FORWARD_AUTH:
    label: federation forward auth
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_HTTP_TIMEOUT_MS:
    label: federation http timeout ms
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_LOAD_AWARE_READS:
    label: federation load aware reads
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_LOAD_AWARE_READ_PRESSURE:
    label: federation load aware read pressure
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_MAX_MIRROR_STALENESS_MS:
    label: federation max mirror staleness ms
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_MAX_REPOS:
    label: federation max repos
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_MODE:
    label: federation mode
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_NODE_BASE_URL:
    label: federation node base url
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_NODE_ID:
    label: federation node id
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_NODE_PRIVATE_KEY_PATH:
    label: federation node private key path
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_NODE_PUBLIC_KEY_PATH:
    label: federation node public key path
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_PARENTS:
    label: federation parents
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_READ_FROM_MIRRORS:
    label: federation read from mirrors
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_REMOTE_LOAD_TTL_MS:
    label: federation remote load ttl ms
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_SPILLOVER_MAX_ATTEMPTS:
    label: federation spillover max attempts
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_TRANSITIVE_DISCOVERY:
    label: federation transitive discovery
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_TRANSITIVE_TRUST:
    label: federation transitive trust
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_WRITE_PROXY_ENABLED:
    label: federation write proxy enabled
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_FEDERATION_WRITE_PROXY_MAX_HOPS:
    label: federation write proxy max hops
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GIT_EXTERNAL_TRANSPORT_ENABLED:
    label: git external transport enabled
    group: git
    description: TreeDX-owned git environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GIT_SECRET:
    label: git secret
    group: git
    description: TreeDX-owned git environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GIT_SSH_ENABLED:
    label: git ssh enabled
    group: git
    description: TreeDX-owned git environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GIT_SSH_KNOWN_HOSTS:
    label: git ssh known hosts
    group: git
    description: TreeDX-owned git environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_INCREMENTAL_MAX_CHANGED_PATHS:
    label: graph incremental max changed paths
    group: graph
    description: TreeDX-owned graph environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_INDEX_CACHE_ENABLED:
    label: graph index cache enabled
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_INDEX_CACHE_MAX_BYTES:
    label: graph index cache max bytes
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_INDEX_CACHE_MAX_ENTRIES:
    label: graph index cache max entries
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_INDEX_CACHE_MEMORY_WEIGHT:
    label: graph index cache memory weight
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_INDEX_CACHE_TTL_MS:
    label: graph index cache ttl ms
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_MAX_QUEUE:
    label: graph max queue
    group: graph
    description: TreeDX-owned graph environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_QUEUE_TIMEOUT_MS:
    label: graph queue timeout ms
    group: graph
    description: TreeDX-owned graph environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_REFRESH_DEDUPE_TIMEOUT_MS:
    label: graph refresh dedupe timeout ms
    group: graph
    description: TreeDX-owned graph environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_GRAPH_WORKER_POOL_SIZE:
    label: graph worker pool size
    group: graph
    description: TreeDX-owned graph environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_HEALTH_CHECK_AUTH_PROVIDER:
    label: health check auth provider
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_HEALTH_CHECK_TIMEOUT_MS:
    label: health check timeout ms
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_HEAVY_OPERATION_EXECUTION_TIMEOUT_MS:
    label: heavy operation execution timeout ms
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_HEAVY_OPERATION_QUEUE_TIMEOUT_MS:
    label: heavy operation queue timeout ms
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_IMAGE_REF:
    label: image ref
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_IMPORT_MAX_QUEUE:
    label: import max queue
    group: import
    description: TreeDX-owned import environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_IMPORT_QUEUE_TIMEOUT_MS:
    label: import queue timeout ms
    group: import
    description: TreeDX-owned import environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_IMPORT_WORKER_POOL_SIZE:
    label: import worker pool size
    group: import
    description: TreeDX-owned import environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWKS_CACHE_TTL_SECONDS:
    label: jwks cache ttl seconds
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWKS_REFRESH_TIMEOUT_MS:
    label: jwks refresh timeout ms
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWKS_ROTATION_GRACE_SECONDS:
    label: jwks rotation grace seconds
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWKS_URL:
    label: jwks url
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWT_ALLOWED_ALGS:
    label: jwt allowed algs
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWT_AUDIENCE:
    label: jwt audience
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWT_CLOCK_SKEW_SECONDS:
    label: jwt clock skew seconds
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWT_HS256_SECRET:
    label: jwt hs256 secret
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_JWT_ISSUER:
    label: jwt issuer
    group: auth
    description: TreeDX-owned auth environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_KEEP_RUNNING:
    label: keep running
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_LIVE_NODE_A_REPO_ID:
    label: live node a repo id
    group: live-test
    description: TreeDX-owned live-test environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_LIVE_NODE_A_TOKEN:
    label: live node a token
    group: live-test
    description: TreeDX-owned live-test environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_LIVE_NODE_A_URL:
    label: live node a url
    group: live-test
    description: TreeDX-owned live-test environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_LIVE_NODE_B_REPO_ID:
    label: live node b repo id
    group: live-test
    description: TreeDX-owned live-test environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_LIVE_NODE_B_TOKEN:
    label: live node b token
    group: live-test
    description: TreeDX-owned live-test environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_LIVE_NODE_B_URL:
    label: live node b url
    group: live-test
    description: TreeDX-owned live-test environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_MAX_BLOB_BYTES:
    label: max blob bytes
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_MAX_FILE_BYTES:
    label: max file bytes
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_MAX_MULTIPART_BLOB_BYTES:
    label: max multipart blob bytes
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_MULTIPART_PART_BYTES:
    label: multipart part bytes
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_NODE_A_HOST_PORT:
    label: node a host port
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_NODE_ID:
    label: node id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_OPENAPI_PATH:
    label: openapi path
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILER_FIXTURE_ROOT:
    label: profiler fixture root
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILER_ROOT:
    label: profiler root
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_BASE_URL:
    label: profile base url
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_CONCURRENCY:
    label: profile concurrency
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_DELAYED_CHECK_INTERVALS:
    label: profile delayed check intervals
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_DELAYED_CONSISTENCY_CHECKS:
    label: profile delayed consistency checks
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_DURATION:
    label: profile duration
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_DURATION_IS_CONTROLLING:
    label: profile duration is controlling
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FAILURE_REPLAY_LOG:
    label: profile failure replay log
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FAIL_BELOW_PRIMARY_RPS:
    label: profile fail below primary rps
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FAULT_INJECTION:
    label: profile fault injection
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_EXERCISE_CONNECTED_DENIALS:
    label: profile federation exercise connected denials
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_EXERCISE_PROMOTION:
    label: profile federation exercise promotion
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_EXERCISE_WRITE_PROXY:
    label: profile federation exercise write proxy
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_MODE:
    label: profile federation mode
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_NODE_A_URL:
    label: profile federation node a url
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_NODE_B_URL:
    label: profile federation node b url
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_NODE_C_URL:
    label: profile federation node c url
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FEDERATION_SPILLOVER_PROBE:
    label: profile federation spillover probe
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FIXTURE:
    label: profile fixture
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_FIXTURE_ROOT:
    label: profile fixture root
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_GRAPH_REFRESH_RATE:
    label: profile graph refresh rate
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_HEAVY_OPERATION_RATE:
    label: profile heavy operation rate
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_HOST_GID:
    label: profile host gid
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_HOST_UID:
    label: profile host uid
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_IMPORT_RATE:
    label: profile import rate
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_INCLUDE_ADMIN:
    label: profile include admin
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_INCLUDE_DESTRUCTIVE:
    label: profile include destructive
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_INCLUDE_EXEC:
    label: profile include exec
    group: execution
    description: TreeDX-owned execution environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_INCLUDE_FEDERATION:
    label: profile include federation
    group: federation
    description: TreeDX-owned federation environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_INCLUDE_PROBE_SAMPLES:
    label: profile include probe samples
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_INCLUDE_TOTAL_THROUGHPUT:
    label: profile include total throughput
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_ITERATIONS:
    label: profile iterations
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_LOAD_MODE:
    label: profile load mode
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_MARKDOWN_OUTPUT:
    label: profile markdown output
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_METAMORPHIC_CHECKS:
    label: profile metamorphic checks
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_MINIMUM_MEASURED_DURATION:
    label: profile minimum measured duration
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_MODEL_RECONCILIATION:
    label: profile model reconciliation
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_NEGATIVE_TESTS:
    label: profile negative tests
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_OPENAPI_RESPONSE_VALIDATION:
    label: profile openapi response validation
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_OPERATION_CHAINS:
    label: profile operation chains
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_OUTPUT:
    label: profile output
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PERFORMANCE_WORKLOAD:
    label: profile performance workload
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PERMISSION_MATRIX:
    label: profile permission matrix
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PORTFOLIO_GROWTH_TARGET:
    label: profile portfolio growth target
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PORTFOLIO_INITIAL_REPOS:
    label: profile portfolio initial repos
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PORTFOLIO_MAX_REPOS:
    label: profile portfolio max repos
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PORTFOLIO_MIN_REPO_AGE_BEFORE_DELETE:
    label: profile portfolio min repo age before delete
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PROBE_SAMPLING_RATE:
    label: profile probe sampling rate
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_PROFILE_PURPOSE:
    label: profile profile purpose
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_RECONCILIATION_INTERVAL:
    label: profile reconciliation interval
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_RECONCILIATION_SAMPLE_SIZE:
    label: profile reconciliation sample size
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_RELIABILITY_BUDGET:
    label: profile reliability budget
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_RELIABILITY_VERIFIER:
    label: profile reliability verifier
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_REPLAY_LOG:
    label: profile replay log
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_REPORT_FORMAT:
    label: profile report format
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_REPO_GROWTH_RATE:
    label: profile repo growth rate
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_REPO_PREFIX:
    label: profile repo prefix
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_RESTART_DURABILITY_CHECK:
    label: profile restart durability check
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_SCENARIO:
    label: profile scenario
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_SIZE:
    label: profile size
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_SNAPSHOT_RATE:
    label: profile snapshot rate
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_TARGET_PRIMARY_RPS:
    label: profile target primary rps
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_TIMEOUT_MS:
    label: profile timeout ms
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROFILE_VALIDATION_PROBE_MODE:
    label: profile validation probe mode
    group: profile
    description: TreeDX-owned profile environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROXY_ACTOR_ID:
    label: proxy actor id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_PROXY_TENANT_ID:
    label: proxy tenant id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REF:
    label: ref
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REGISTRY_MODE:
    label: registry mode
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REMOTE_CREDENTIALS_FILE:
    label: remote credentials file
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REMOTE_CREDENTIAL_COMMAND:
    label: remote credential command
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REMOTE_CREDENTIAL_PROVIDER:
    label: remote credential provider
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPOSITORY_QUERY_MAX_QUEUE:
    label: repository query max queue
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPOSITORY_QUERY_POOL_SIZE:
    label: repository query pool size
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPOSITORY_QUERY_QUEUE_TIMEOUT_MS:
    label: repository query queue timeout ms
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPOSITORY_STORAGE_MIGRATION:
    label: repository storage migration
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPO_DOC_CACHE_ENABLED:
    label: repo doc cache enabled
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPO_DOC_CACHE_MAX_BYTES:
    label: repo doc cache max bytes
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPO_DOC_CACHE_MAX_ENTRIES:
    label: repo doc cache max entries
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPO_DOC_CACHE_MEMORY_WEIGHT:
    label: repo doc cache memory weight
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_REPO_DOC_CACHE_TTL_MS:
    label: repo doc cache ttl ms
    group: cache
    description: TreeDX-owned cache environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_ROOT_DIR:
    label: root dir
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_RUNTIME_CPU_BUDGET:
    label: runtime cpu budget
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_RUNTIME_MEMORY_BUDGET_MB:
    label: runtime memory budget mb
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SDK_BASE_URL:
    label: sdk base url
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SDK_TOKEN:
    label: sdk token
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SMOKE_COMPOSE_FILE:
    label: smoke compose file
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SNAPSHOT_MAX_FILE_BYTES:
    label: snapshot max file bytes
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SNAPSHOT_MAX_QUEUE:
    label: snapshot max queue
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SNAPSHOT_MAX_TOTAL_BYTES:
    label: snapshot max total bytes
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SNAPSHOT_QUEUE_TIMEOUT_MS:
    label: snapshot queue timeout ms
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_SNAPSHOT_WORKER_POOL_SIZE:
    label: snapshot worker pool size
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_STORAGE_FORMAT_VERSION:
    label: storage format version
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_STORAGE_MODE:
    label: storage mode
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_STORAGE_RESTORE_ACK:
    label: storage restore ack
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_STORAGE_RESTORE_ENABLED:
    label: storage restore enabled
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TARGET_DIR:
    label: target dir
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TOKEN:
    label: token
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TOOLS_DIR:
    label: tools dir
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_API_BASE_URL:
    label: treeseed api base url
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_ASSIGNMENT_ID:
    label: treeseed assignment id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_CREDENTIAL_OPERATION:
    label: treeseed credential operation
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_GITHUB_INSTALLATION_ID:
    label: treeseed github installation id
    group: git
    description: TreeDX-owned git environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_PATHS:
    label: treeseed paths
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_PROJECT_ID:
    label: treeseed project id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_PROVIDER_ID:
    label: treeseed provider id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_REF:
    label: treeseed ref
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_REPOSITORY:
    label: treeseed repository
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_SERVICE_ID:
    label: treeseed service id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_SERVICE_SECRET:
    label: treeseed service secret
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: secret
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_TEAM_ID:
    label: treeseed team id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_TREESEED_WORKDAY_ID:
    label: treeseed workday id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_UPLOAD_SESSION_TTL_SECONDS:
    label: upload session ttl seconds
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_URL:
    label: url
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_WORKSPACE_ID:
    label: workspace id
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_WORKSPACE_MUTATION_MAX_QUEUE:
    label: workspace mutation max queue
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_WORKSPACE_MUTATION_QUEUE_TIMEOUT_MS:
    label: workspace mutation queue timeout ms
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
  TREEDX_WORKSPACE_WORKER_POOL_SIZE:
    label: workspace worker pool size
    group: runtime
    description: TreeDX-owned runtime environment setting.
    howToGet: Configure according to the TreeDX runtime, test, or deployment surface that consumes this variable.
    sensitivity: plain
    targets:
      - local-runtime
    scopes:
      - local
      - staging
      - prod
    storage: scoped
    requirement: optional
    purposes:
      - dev
      - deploy
      - config
    validation:
      kind: nonempty
    sourcePriority:
      - machine-config
      - process-env
