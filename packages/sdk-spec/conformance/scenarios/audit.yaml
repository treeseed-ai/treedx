version: 0.1.0
status: draft
scenarios:
- id: audit.events
  capabilityId: audit.events
  title: Audit / Events
  kind: black_box
  required: true
  serverRequired: true
  endpointRefs:
  - GET /api/v1/audit/events
  fixtures:
    repos: []
    requests: []
    expected: []
  steps:
  - name: execute
    action: audit.events
    description: Execute audit.events through the public SDK facade against the local TreeDX conformance harness.
  assertions:
  - The SDK dispatches the declared OpenAPI operation through its transport/auth/error stack.
  - Responses and TreeDX error envelopes are exposed without product-specific semantics.
