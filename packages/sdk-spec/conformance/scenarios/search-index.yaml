version: 0.1.0
status: draft
scenarios:
- id: search_index.refresh_status_compact
  capabilityId: search_index.lifecycle
  title: Search Index / Refresh Status Compact
  kind: black_box
  required: true
  serverRequired: true
  endpointRefs:
  - GET /api/v1/repos/{repo_id}/search/index/status
  - POST /api/v1/repos/{repo_id}/search/index/refresh
  - POST /api/v1/repos/{repo_id}/search/index/compact
  fixtures:
    repos: []
    requests: []
    expected: []
  steps:
  - name: execute
    action: search_index.refresh_status_compact
    description: Execute search_index.refresh_status_compact through the public SDK facade against the local TreeDX conformance harness.
  assertions:
  - The SDK dispatches the declared OpenAPI operation through its transport/auth/error stack.
  - Responses and TreeDX error envelopes are exposed without product-specific semantics.
