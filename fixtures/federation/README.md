# Federation Fixtures

MVP federation coverage is planner-only. Tests request multiple repositories and assert TreeDX reduces the requested scope before any query or search execution.

No live multi-node deployment fixture is committed here.
