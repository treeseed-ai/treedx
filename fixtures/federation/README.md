# Federation Fixtures

Phase 10 federation coverage is planner-only. Tests request multiple repositories and assert TreeDB reduces the requested scope before any query or search execution.

No live multi-node deployment fixture is committed here.
