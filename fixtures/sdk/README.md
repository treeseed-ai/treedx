# SDK Fixtures

SDK Phase 10 tests use mocked fetch responses to prove remote TreeDB mode does not require an agent-side repository clone when `contentPathMap` is configured.

Live SDK contract tests are opt-in through environment variables and are skipped by default.
