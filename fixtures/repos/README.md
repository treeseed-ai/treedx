# Repository Fixtures

MVP tests create temporary Git repositories dynamically. This directory documents the fixture contract only; it does not contain committed Git repositories or binary artifacts.

The default repository shape includes Markdown, MDX, plain text, headings, frontmatter, local links, unresolved `treedx://` references, and restricted-path content for authorization checks.
